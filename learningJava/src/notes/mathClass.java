package notes;

public class mathClass {
    public static void main(String[] args) {

        // THE CONSTANT OF OF PI
        //System.out.print(Math.PI);

        // Exponential Constant or Eulers Number
        //System.out.print(Math.E);

        double result;

        result = Math.pow(2, 3);

        // Gives that number in positive, distance away from 0
        result = Math.abs(-5);

        result = Math.sqrt(9);
        result = Math.round(4.5567);


        // Rounds upwards
        result = Math.ceil(3.14);

        // Round downwards
        result = Math.floor(3.56);

        result = Math.max(10, 20);
        result = Math.min(10, 20);
        System.out.print(result);
    }
}
